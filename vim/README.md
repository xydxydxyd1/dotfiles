# (Neo)vim configurations

- `basic.vim` contains basic setup that only needs base vim
- Plugins for base vim are phased out in favor for Lua plugins for Neovim

## Setup

- Create `lua/config/local.lua`
